# 18 — Audit Refresh (2026-04-29)

> **Refresh of the 2026-04-22 monorepo audit.** This document supersedes the per-finding
> status across docs `00`–`17`. The original docs are preserved as the historical baseline;
> consult them for deeper background. This refresh focuses on **what has changed** in the
> ~47 commits since the previous audit and on the **current** health of the project.

| | Previous audit | This refresh |
|---|---|---|
| Date | 2026-04-22 | **2026-04-29** |
| Base SHA | `dd22666efb42e7f3f2d7e83a9f57ab7c8123852e` | `9c18dafa1c7d620e6246692e90071e722f9a314b` |
| Branch | `copilot/audit-monorepo-analysis` | `copilot/update-audit-repo` |
| Commits since baseline | — | **47** (+11,964 / −1,351 lines across 143 files) |
| Builds re-run | ✅ | ✅ `:mobile:assembleDebug` (132 MB), `:wear:assembleDebug` (26 MB), `:mobile:testDebugUnitTest` (138/138), `:shared:test` (6/6) |

> ⚠️ **No production code was modified.** This is a read-only refresh.

---

## 1. Headline

The project has **substantially closed the most painful 2026-04-22 findings**. Two of the
three 🔴 criticals are fully fixed; the third (Realm/Room duality) is now an *acknowledged,
documented, controlled* mid-migration rather than a silent footgun. Mobile↔Wear integration
went from "documented but not implemented" to **"implemented and source-validated"**. The
voice-analysis screen no longer ships fabricated formant numbers.

**However**, three new findings worth flagging up front:

- 🔴 **`:mobile:lintDebug` currently fails** with 2 errors (both in `HomeScreen.kt`, lines 133 & 137 — `context.getString` inside a `@Composable`). With the newly-enforced `abortOnError true`, this **breaks any release-side CI pass that runs lint**. (ISSUE-035)
- 🟡 **Mobile debug APK ballooned from ~74 MB to 132 MB** since the prior audit. Worth attributing before the next release. (ISSUE-036)
- 🟢 **Material3 alpha pin relaxed (ISSUE-029 — resolved 2026-04-29).** `mobile/build.gradle` now uses `strictly '[1.5.0-alpha18,)'` instead of an exact pin, so future M3 versions are picked up automatically.

| Score | Tier |
|------:|------|
| **6.5 / 10** ↑ from 5 | Functional, with a couple of known migration debts. Shippable on the mobile side; Wear is now usable but architecturally still XML+Views with dead Compose deps. |

### Refreshed sub-scores

| Area | Prev | Now | Note |
|------|-----:|----:|------|
| 📱 Mobile app | 6 | **7** | Quality-polish sprint (`f515817`) closed ISSUE-003/007/009/010/011/015/017/023/026; HorizontalFloatingToolbar refactor; honest voice DSP; lint enforcement on. |
| ⌚ Wear OS app | 3 | **5** | Capabilities now registered; `ChannelClient` audio transport; PARTIAL_WAKE_LOCK correctly scoped; tests added (in `:shared`). UI still XML; `WearTheme.kt` still dead. |
| 🔗 Shared module | 6 | **7** | First real test (`WearableHelperTest`); CONTRIBUTING.md added. Schema-versioning gap remains. |
| 🔁 Mobile↔Wear integration | 2 | **7** | Capability discovery works; messages source-validated; audio is sanitized + canonicalized; `handleSyncRequest` actually syncs milestones. |
| 🔐 Security posture | 6 | **7** | Source-node validation, path-traversal hardening, fail-loud release signing. Encryption toggle still disabled — but now *honestly* (returns false unconditionally with a comment explaining why). |
| 🧪 Testing & CI | 5 | **6** | +3 real test files (PitchTracker, SessionSummaryBuilder, WearableHelper); CI churn reduced minutes spend; `lint { abortOnError true }` now enforced. AudioDecoder/AudioAnalysisUtil/Realm→Room migration still untested. |
| 📚 Documentation | 4 | **6** | New CONTRIBUTING.md, DEFERRED_WORK.md, README rewrite (commit `94e9cef`); voice DSP gets KDoc; mkdocs site live. README still says voice values are "typical estimates" though pitch is now real. |
| 📦 Dependencies | 6 | **6** | Picasso removed; Compose BOM bumped; Material3 pinned to `1.5.0-alpha18` (intentional). Archived `firebase-ui-auth 8.0.2` and `sqlcipher 4.5.4` (deprecated GAV) remain. |

---

## 2. Status of the 5 previous "criticals"

| # | Finding (2026-04-22) | Status (2026-04-29) | Evidence |
|---|---|---|---|
| 1 | 🔴 Wearable capabilities never registered | ✅ **Resolved** | `wear/src/main/res/values/wear.xml:10-12` declares `opentransition_wear_app`; `mobile/src/main/res/values/wear.xml:15-17` declares `opentransition_mobile_app`; constants match `shared/.../WearableConstants.kt:19-20`; `wear/.../MainActivity.kt:116` calls `getCapability(CAPABILITY_MOBILE_APP, FILTER_REACHABLE)` and now returns nodes. |
| 2 | 🔴 Play Store deploy uploads to wrong package name | ✅ **Resolved** | `.github/workflows/ci.yml:76` uses `com.shelbeely.opentransition` (mobile), `:85` uses `com.shelbeely.opentransition.wear`. `grep -r "com.drspaceboo" .github/ fastlane/` → 0 hits. |
| 3 | 🔴 Two databases run side-by-side; UI still on Realm | 🟡 **Partial / acknowledged** | Realm still imported by 22 files (legacy adapters, domain layer); Room owns 9 (DAOs/entities incl. new `VoiceGoalEntity/Dao`). The dual write happens for `RecordAudioFragment` (`:254-271` Realm + `:273-301` Room). The "encrypted DB" toggle is now **honestly disabled** (`SettingsManager.kt:408` returns `false` unconditionally with a KDoc citing this audit) so the misleading-Settings-claim portion is closed. |
| 4 | 🟠 `AudioAnalysisUtil` returns hard-coded fake formants | 🟡 **Honest fix** | `AudioAnalysisUtil.kt:19-30` docs explicitly state pitch is real and formants are **zeroed** because LPC is not implemented. Pitch flows through real DSP (`PitchTracker.kt` autocorrelation; `AudioDecoder.kt` MediaCodec). Formants no longer faked, just absent. README still labels the feature "preview" with a slightly outdated "typical estimates" caveat. |
| 5 | 🟠 Wear app uses XML Views; `WearTheme.kt` is dead code | ⚠️ **Same risk, more code** | `WearTheme.kt` has been *expanded* (158-175) with proper M3 palettes/typography but still has **zero callers**. All three Wear activities still inflate XML (`MainActivity.kt:58`, `AudioRecordActivity.kt:81`, `CameraControlActivity.kt:42`). Compose-for-Wear classpath still adds ~3 MB to the AAB. |

**Summary:** 2 fully resolved, 1 controlled (with the most user-visible aspect closed), 1 partially fixed honestly, 1 unchanged.

---

## 3. New since 2026-04-22

### 3.1 New code paths

11 new Kotlin files (1,550 LOC) form a vertical voice-analysis slice:

| Layer | Files |
|-------|-------|
| DSP / utilities | `util/PitchTracker.kt` (192 LOC, autocorrelation F0 estimator), `util/AudioDecoder.kt` (138 LOC, MediaCodec→PCM), `util/SessionSummaryBuilder.kt` (104 LOC, aggregates), `util/VoiceMetric.kt` (33 LOC, enum) |
| Data | `database/room/entities/VoiceGoalEntity.kt`, `database/room/dao/VoiceGoalDao.kt`, `domain/VoiceGoalRepository.kt`, `mobile/schemas/.../AppDatabase/3.json` |
| UI (Compose) | `ui/voiceprogress/VoiceProgressFragment.kt` + `VoiceProgressScreen.kt`, `ui/voicesession/VoiceSessionDetailFragment.kt` + `VoiceSessionDetailScreen.kt`, `ui/voicegoal/GoalCreationSheet.kt`, `ui/widget/SpectrogramView.kt` (438-LOC custom view, allocations hoisted) |

Plus the `HorizontalFloatingToolbar` + speed-dial FAB refactor on `HomeScreen` (commit `f361976`).

### 3.2 New / changed governance docs

- **`DEFERRED_WORK.md`** (new) — explicitly tracks ISSUE-008, 012, 013, 014, 016, 018 as deferred, with current status and rationale. This is itself a positive process change.
- **`CONTRIBUTING.md`** (new, commit `1226a8d`).
- **`README.md`** rewritten (commit `94e9cef`) with TOC, tech stack, project structure, corrected SDK versions.
- **`mkdocs.yml`** + `docs/` tree publishing to GitHub Pages (deploy-docs.yml).

### 3.3 New / changed CI

| Workflow | Change |
|---|---|
| `ci.yml` | Now `push:production`-only (no per-PR runs); deploys both mobile and wear AABs in one job. |
| `build-release.yml` | Tag-trigger removed; `workflow_dispatch` only. |
| `pr-debug.yml` | Now `workflow_dispatch` only. |
| `boycott-check.yml` | Path-filtered to gradle/yaml/script changes. |
| `copilot-debug-build.yml` | **NEW** — fast-path debug APK build for Copilot sessions. |
| `copilot-setup-steps.yml` | Refactored; stub-secrets/dummy-google-services/AVD bootstrap verified working. |
| Concurrency caps added on `ci`, `ui-screenshots`, `deploy-docs`, `boycott-check`. |

Net effect: significant Actions-minutes savings (commit `0bf338c`) without losing release-deploy automation.

---

## 4. Refreshed issue catalogue

### 4.1 Closed since baseline (✅ verified fixed)

| ID | Title | Where fixed |
|----|-------|------------|
| ISSUE-001 | Wear capabilities not registered | `wear.xml` files added in both modules |
| ISSUE-002 | Wrong Play Store packageName in CI | `.github/workflows/ci.yml:76,85` |
| ISSUE-003 | `handleSyncRequest` was a stub | Now actually syncs milestones via `WearableHelper` |
| ISSUE-007 | `WearableListenerService` handler bodies empty | Filled (`f515817`) |
| ISSUE-009 | `Handler.postDelayed` leak in Wear `AudioRecordActivity` | Cleanup added (`f515817`) |
| ISSUE-010 | Audio shipped via `DataClient` (100 KB cap) | Migrated to `ChannelClient` (`MobileWearableListenerService.kt:109-119`, `wear/.../AudioRecordActivity.kt:54,83,240`) |
| ISSUE-011 | No source-node validation | `MobileWearableListenerService.kt:62-93` validates against capability set; rejects unknown senders |
| ISSUE-015 | Unused `navigation-compose` dependency | Removed from `mobile/build.gradle` |
| ISSUE-017 | Hard-coded UI strings | Moved to `strings.xml` (`f515817`) |
| ISSUE-019 | Wear AAB not bundled | `:wear:bundleRelease` invoked in `ci.yml:70` |
| ISSUE-020 | CI secret name drift | Standardised in `copilot-setup-steps.yml` |
| ISSUE-023 | Eager `Application.onCreate` work on main thread | `TransTracksApp.kt:65,78-82` uses `appScope.launch(Dispatchers.IO)` |
| ISSUE-026 | `lint { abortOnError true }` not enforced | `mobile/build.gradle:138` |
| ISSUE-028 / sec | Path-traversal in incoming audio filename | `MobileWearableListenerService.kt:130` canonical-path check |
| Sign-config | Release signing failed silently | Now fail-loud; `mobile/build.gradle:53-90` raises `GradleException` if release-keystore or env vars missing on a release build |

### 4.2 Carried forward (still open)

| ID | Severity | Title | Current status |
|----|----------|-------|----------------|
| ISSUE-004 | 🟠 (was 🔴) | Realm + Room dual-write, UI predominantly on Realm | Toggle is honest now, but the migration is still in flight. Tracked in `DEFERRED_WORK.md`. |
| ISSUE-005 | 🟡 | Voice analysis claims vs implementation | Pitch real; formants intentionally absent. README copy lags ("preview"/"typical estimates" wording outdated). |
| ISSUE-008 | 🟡 | Wear milestone cache only in foreground | DEFERRED_WORK.md item; not yet implemented |
| ISSUE-012 | 🟠 | `Realm.openDefault()` on main thread inside adapters | `HomeGalleryAdapter.kt:58`, `MilestonesAdapter.kt:38`, `GalleryAdapter.kt` (similar). Cold-start jank vector. |
| ISSUE-013 | 🟡 | `MobileWearableListenerService` writes to a Realm with inline config bypassing `DatabaseManager` | Lines ~172-188; ISSUE-004 sub-case |
| ISSUE-014 | 🟢 | `lifecycleScope.launch(Dispatchers.IO)` retains fragment until IO finishes | DEFERRED_WORK.md item; SettingsFragment refactor deferred |
| ISSUE-016 | 🟡 | `WearTheme.kt` + Compose-for-Wear classpath dead (~3 MB AAB bloat) | Theme expanded but still 0 callers |
| ISSUE-018 | 🟢 | Legacy `/app/` paths in `.gitignore` | Cosmetic |
| ISSUE-022 | 🟡 | `SettingsFragment.kt` god object (now 961 LOC) | Larger than at baseline |
| ISSUE-024 | 🟡 | `WEAR_APP_FEATURES.md` still oversells | Needs trimming to current capability set |

### 4.3 New issues introduced since baseline

| ID | Severity | Title | Evidence |
|----|----------|-------|----------|
| **ISSUE-029** | ✅ **Resolved 2026-04-29** | **Material3 pin relaxed from exact-version to range.** Originally `strictly '1.5.0-alpha18'` (exact); now `strictly '[1.5.0-alpha18,)'`. Resolves to `1.5.0-alpha18` today (still the highest published) but will automatically pick up future alphas, betas, `1.5.0` stable, and later majors as the Compose BOM advances. Verified with `./gradlew :mobile:dependencies` (resolution still pins to `1.5.0-alpha18`) and `:mobile:assembleDebug` (BUILD SUCCESSFUL). | `mobile/build.gradle:153-167` |
| **ISSUE-030** | 🟢 | **`androidx.activity:activity-ktx` version skew between modules.** mobile uses 1.9.3, wear uses 1.7.2. Risk is low (no shared classpath), but easy to align. | `mobile/build.gradle` vs `wear/build.gradle` |
| **ISSUE-031** | 🟢 | **README's voice-analysis section labels pitch as "typical estimate"** even though `PitchTracker` now performs real autocorrelation. Mismatch between docs and code. | `README.md:75` vs `util/PitchTracker.kt` + `util/AudioAnalysisUtil.kt:19-30` |
| **ISSUE-032** | 🟢 | **No KDoc on new voice UI screens** (`VoiceProgressScreen.kt`, `GoalCreationSheet.kt`). DSP utilities are well-documented; UI layer is sparse. | `ui/voiceprogress/`, `ui/voicegoal/` |
| **ISSUE-033** | 🟡 | **`AudioDecoder` and `AudioAnalysisUtil` have no unit tests.** PitchTracker and SessionSummaryBuilder do. The decoder is a likely source of edge-case bugs (sample rate, channel layout, mono/stereo, MediaCodec quirks). | `mobile/src/test/java/...` |
| **ISSUE-034** | 🟡 | **`androidx.compose.material:material-icons-extended` added without confirming actual usage.** Easy to bloat AAB by hundreds of KB. Verify with R8/ProGuard report. | `mobile/build.gradle:165` |
| **ISSUE-035** | 🔴 | **`:mobile:lintDebug` fails — 2 errors, 188 warnings.** Two `LocalContextGetResourceValueCall` errors at `ui/home/HomeScreen.kt:133` and `:137` use `context.getString(R.string.…)` inside a `@Composable` (use `stringResource(…)` instead). Combined with ISSUE-026's `abortOnError true`, this **breaks any CI workflow that runs lint** until fixed (or a baseline file is created). | `mobile/build/reports/lint-results-debug.txt`; lint output: `BUILD FAILED in 6m 3s … Lint found 2 errors, 188 warnings.` |
| **ISSUE-036** | 🟡 | **Mobile debug APK ballooned from ~74 MB → 132 MB** since baseline. Likely contributors: voice-analysis assets, `material-icons-extended`, Coil 3 + OkHttp, Compose Material3 alpha. Run `./gradlew :mobile:bundleRelease` + `apkanalyzer` to attribute. | `mobile/build/outputs/apk/debug/mobile-debug.apk` (132 MB) vs prior audit doc 17 (74 MB) |

(IDs continue from the prior catalogue's last assigned `ISSUE-028`.)

---

## 5. Refreshed dependency picture

### 5.1 Notable upgrades / additions / removals (since `dd22666`)

| Change | Coordinate | Note |
|--------|-----------|------|
| Removed | `com.squareup.picasso:picasso 2.8` | All sites migrated to Coil 3.1.0 (commit `6c01694`). 0 `import com.squareup.picasso` remain. |
| Removed | `androidx.navigation:navigation-compose 2.8.5` | Was unused. |
| Upgraded | `androidx.compose:compose-bom` `2025.05.01 → 2025.11.01` | Routine bump. |
| Pinned (range) | `androidx.compose.material3:material3 [1.5.0-alpha18,)` (`strictly`) | For `HorizontalFloatingToolbar`. Originally an exact-version pin; relaxed to a range on 2026-04-29 (ISSUE-029 resolved). Resolves to `1.5.0-alpha18` today. |
| Added | `com.google.android.play:review[-ktx] 2.0.1` | In-app review (ITEM-50). |
| Added | `androidx.compose.material:material-icons-extended` (BOM) | New ISSUE-034 (verify usage). |

### 5.2 Top "watch list" deps (rank by likely-CVE / staleness)

1. 🔴 **`com.firebaseui:firebase-ui-auth 8.0.2`** — project archived (2024); migrate to FirebaseAuth + Credential Manager.
2. 🟠 **`net.zetetic:android-database-sqlcipher 4.5.4`** — deprecated GAV, rebranded to `net.zetetic:sqlcipher-android` in 4.6+. Migration is a non-trivial Room SupportFactory swap; reverted in commit `6c01694`.
3. 🟠 **`androidx.security:security-crypto 1.1.0-alpha06`** — alpha in production. Used by `KeystoreManager`.
4. 🟡 **`com.google.android.gms:play-services-ads 22.4.0`** — 1+ year old; 23.6+ available.
5. 🟡 **`com.google.android.gms:play-services-auth 20.7.0`** — superseded by Credential Manager.

### 5.3 Renovate / Dependabot

`.github/dependabot.yml` enabled (gradle, weekly, reviewer `shelbeely`). Recommendation unchanged from prior audit: broaden reviewer pool / consider auto-merge for safe patch updates.

---

## 6. Refreshed performance & runtime notes

| Concern | Status | Evidence |
|---------|--------|----------|
| Cold-start jank from `Application.onCreate` | ✅ Fixed | `TransTracksApp.kt:78-82` off-thread |
| Cold-start jank from `Realm.openDefault` on main | 🟡 Partial | Top-level fixed; **adapters still open Realm on main** (ISSUE-012) |
| Wear `Handler.postDelayed` leak | ✅ Fixed | ISSUE-009 |
| Wear `PARTIAL_WAKE_LOCK` overuse | ✅ Correct | `wear/.../AudioRecordActivity.kt:144,197` acquired during recording, released in `onPause` |
| Voice DSP on main thread | ✅ Off-thread | `SpectrogramView.kt:128-154` decodes in `Thread{}`; fragments use `Dispatchers.IO` |
| `SpectrogramView.onDraw` allocations | ✅ Hoisted | All `Paint`/`Path` declared as fields; `pitchPath.reset()` reused (commit `ca66717`) |
| Image loading on main / no down-sampling | 🟡 Open | Coil default config; `GalleryAdapter` loads full JPEGs into thumbnails |
| AAB bloat from dead Wear-Compose | 🟡 Open | ISSUE-016 |

---

## 7. Refreshed test & CI signal

| Check | Result this refresh |
|-------|--------------------|
| `:mobile:assembleDebug` | ✅ APK 132 MB (vs 74 MB at baseline — voice DSP, Compose icons, Coil; investigate ISSUE-034) |
| `:wear:assembleDebug` | ✅ APK 26 MB (≈baseline) |
| `:mobile:testDebugUnitTest` | ✅ **138 tests, 0 failures, 0 skipped** |
| `:shared:test` | ✅ **6 tests, 0 failures** (new `WearableHelperTest`) |
| `:mobile:lintDebug` | ❌ **FAILED — 2 errors, 188 warnings** (see ISSUE-035). With `abortOnError true` now enforced (`mobile/build.gradle:138`, ISSUE-026 fix), this means **any CI workflow that runs `:mobile:lintDebug` will currently fail on `production`-branch builds.** Both errors are in `mobile/src/main/java/com/shelbeely/opentransition/ui/home/HomeScreen.kt:133,137`: `context.getString(R.string.…)` used inside a `@Composable` instead of `stringResource(…)`. Trivial fix. Other warnings: deprecation on `PreferenceManager`, `Char.toLowerCase`, `ExifInterface.TAG_CAMARA_OWNER_NAME`; `LockedOrientationActivity`, `SelectedPhotoAccess` (Android 14 partial photo access not handled); `AndroidGradlePluginVersion` (Gradle 8.14.4 available). |

> 🔴 **CI risk:** ISSUE-026 (lint enforcement) and ISSUE-035 (the two lint errors) interact. Either fix the two `HomeScreen.kt` errors or temporarily relax `abortOnError` to ship a release build. See ISSUE-035 below.

> ⚠️ APK growth from 74 MB → 132 MB is the single most surprising number. Voice-analysis assets/icons + Coil + `material-icons-extended` are likely contributors. Investigation owner: see ISSUE-034.

Untested high-risk subsystems remain:

- `util/AudioDecoder.kt` (MediaCodec wrapper)
- `util/AudioAnalysisUtil.kt` (orchestrator)
- `database/migration/RealmToRoomMigration.kt` (one-shot, irreversible)
- `database/AppDatabase.kt` (encryption code path is dead, untested)

---

## 8. Refreshed prioritized action plan

### NOW (≤ 1 week)

1. **🔴 Fix `:mobile:lintDebug` failure (ISSUE-035).** Replace the two `context.getString(R.string.…)` calls in `HomeScreen.kt:133,137` with `stringResource(R.string.…)`. Without this, the `lint { abortOnError true }` enforcement (ISSUE-026 fix) blocks any CI that runs lint.
2. **Investigate APK growth (74 MB → 132 MB) (ISSUE-036).** Run `./gradlew :mobile:bundleRelease` and inspect the size report; verify ISSUE-034 (`material-icons-extended`).
3. **Decide and execute ISSUE-004 endgame** — either (a) finish migrating reads to Room and *re-enable* the encryption toggle, or (b) delete the dead Room+SQLCipher code path. The current "honest off" state is fine as a holding pattern but accumulates code-rot.
4. **Fix README voice-analysis copy (ISSUE-031).** Pitch is now real; describe formants as "not yet computed".
5. **Move adapter Realm reads off the main thread (ISSUE-012).** Three adapter files (`HomeGalleryAdapter`, `MilestonesAdapter`, `GalleryAdapter`).
6. **Add unit tests for `AudioDecoder` and `AudioAnalysisUtil` (ISSUE-033).** Synthetic WAV inputs; verify F0 statistics; verify formant fields are zero (regression guard).
7. ~~Document the Material3 alpha pin (ISSUE-029)~~ ✅ **Done 2026-04-29** — pin relaxed from `strictly '1.5.0-alpha18'` to `strictly '[1.5.0-alpha18,)'`; future M3 versions auto-pulled as published.

### NEXT (1 month)

7. **Migrate `firebase-ui-auth` → FirebaseAuth + Credential Manager.**
8. **SQLCipher 4.5.4 → `net.zetetic:sqlcipher-android 4.6+`** (Room SupportFactory swap; was reverted in `6c01694` — needs a second attempt with a real test).
9. **Decide WearTheme fate (ISSUE-016).** Either start migrating Wear UI to Compose or delete the theme + Compose-for-Wear deps to recover ~3 MB AAB.
10. **Bring `:wear` unit tests online** (currently 0 unit tests in `wear/src/test/`). Wrap GMS calls behind interfaces to make them testable.
11. **Schema-version field on every Data Layer payload** (`shared/.../WearableConstants.kt`) — still missing.
12. **Align `androidx.activity:activity-ktx` across modules** (ISSUE-030).
13. **Adopt Gradle version catalog (`libs.versions.toml`)** — would have caught ISSUE-030 automatically.
14. **Wear-side source-node validation** (Phone→Watch trust is still implicit).

### LATER (quarter+)

15. Single-Activity Compose host on mobile + delete remaining XML Activities.
16. Migrate Wear UI to Wear Compose using the (now built) `WearTheme`.
17. Wear Tile + Complication.
18. RxJava → Coroutines + Flow end-to-end migration.
19. Baseline Profile + Macrobenchmark.
20. Hilt or Koin (or formalise the manual DI).
21. Roborazzi screenshot tests.
22. F-Droid `foss` flavor (drop AdMob/Crashlytics/Firebase).
23. Dependency lockfile + verification metadata.
24. Move colour palette + date helpers from `:mobile` into `:shared` (cross-module duplication).

---

## 9. "If I only had 1 week" — refreshed shortlist

If the previous "1-week shortlist" is rerun against current state, items 1–3 and 5 are **already done** and items 4 was *partially* done (toggle is honest, migration is in flight). The new 1-week shortlist:

| # | Action | Why |
|---|--------|-----|
| 1 | Investigate the 74 MB → 132 MB APK growth | Largest single surprise from this refresh |
| 2 | Move adapter Realm calls off the main thread | Concrete cold-start jank vector still present |
| 3 | Decide ISSUE-004 endgame and remove the dead path | Tech-debt amortisation |
| 4 | Update README voice copy + add formant-zero regression test | Truthful marketing + DSP regression guard |
| 5 | Document the Compose Material3 alpha pin | Prevent silent BOM-upgrade breakage |

If those 5 land, projected scores: Mobile 8, Wear 5, Integration 8, Overall 7.0–7.2.

---

## 10. Files updated by this refresh

- `audit-report/README.md` — index updated to point at this refresh.
- `audit-report/CHANGELOG.md` — new file documenting the refresh diff.
- `audit-report/00-executive-summary.md` — header banner directing readers to this doc.
- `audit-report/16-prioritized-action-plan.md` — header banner directing readers to §8 here.
- `audit-report/18-refresh-2026-04-29.md` — this document.

The 17 baseline documents (`00`–`17`) are **unchanged** apart from the small banners on `00`/`16`. They remain the canonical evidence trail for findings as of `dd22666`.
